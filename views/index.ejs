<%- include ('./partials/head.ejs') %>

  <body>
  <div class="body-overlay">
    <!-- ============ HEADER ============ -->
      <%- include ('./partials/header.ejs') %>
        <!-- ============ MAIN ============ -->
        <main>
          <div>
            <h1>Select:
            <ul>
              <% for (let i = 0; i < opera.length; i++) { %>
              <li>
                <a href="/opera/<%= opera[i].id %>">
                  <%=opera[i].name%>
                </a>
              </li>
              <% } %>
            </ul>
            </h1>
          </div>
        </main>

<!-- ============ FOOTER ============ -->
      <%- include ('./partials/footer.ejs') %>
  </div>
  </body>
</html>
